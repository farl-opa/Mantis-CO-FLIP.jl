coverage:
  status:

    project:
      default:   # default is the status check's name, not default settings
        target: auto
        informational: true
    
    patch:
      default:   # default is the status check's name, not default settings
        target: auto
        informational: true